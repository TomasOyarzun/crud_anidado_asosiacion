<p id="notice"><%= notice %></p>

<h1 class="text-center"> Áreas </h1>

<table class="table table-dark">
  <thead>
    <tr>
      <th> Nombre </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @areas.each do |area| %>
      <tr>
        <td><%= area.name %></td>
        <td><%= link_to 'Show', area, class: 'btn btn-primary' %></td>
        <td><%= link_to 'Edit', edit_area_path(area), class: 'btn btn-info' %></td>
        <td>
          <%= link_to 'Destroy', area, class: 'btn btn-danger', method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


